# Copy to .env.migration and fill in. NEVER commit .env.migration.

# SOURCE project (where the data lives now — leave commented if you don't need to re-export)
# SOURCE_URL=https://<source-ref>.supabase.co
# SOURCE_ANON_KEY=<anon key of source project>

# The dealer login (the one whose data you're exporting)
DEALER_EMAIL=<dealer email>
DEALER_PASSWORD=<dealer app password>

# NEW project (fresh)
TARGET_URL=https://fvcafioxkgbljcjomixs.supabase.co
TARGET_SERVICE_ROLE_KEY=<Dashboard -> Settings -> API -> service_role secret>
